# Tragedy NFT - リファクタリング計画

## 現状の問題点

### 1. デプロイメントの混乱
- 4種類の異なるデプロイ方法が混在
- どれを使うべきか不明確
- 各スクリプトが異なる前提条件を持つ

### 2. 状態管理の分散
- デプロイ結果が複数の場所に保存される
- 真実の源泉が不明
- 手動での同期が必要

### 3. ドキュメントの重複
- 3つの異なるデプロイガイドが存在
- 内容が矛盾している
- メンテナンスが困難

## 提案する解決策

### 1. 統一されたデプロイシステム

```
scripts/
├── deploy/
│   ├── main.js          # メインデプロイスクリプト
│   ├── config.js        # 設定管理
│   └── verify.js        # 検証スクリプト
├── utils/               # ユーティリティ
├── test/               # テストスクリプト（分離）
└── archive/            # 廃止されたスクリプト
```

### 2. 単一の設定ファイル

```json
// deploy.config.json
{
  "networks": {
    "bonsoleil": {
      "rpc": "...",
      "chainId": "...",
      "gasPrice": "..."
    }
  },
  "contracts": {
    "order": ["banks", "composer", "metadata", "nft"],
    "verification": true
  },
  "output": {
    "directory": "./deployments",
    "viewer": "./viewer/deployment.json"
  }
}
```

### 3. 明確なデプロイフロー

```bash
# 開発環境
npm run deploy:local

# テストネット
npm run deploy:testnet

# 本番環境
npm run deploy:production

# 検証のみ
npm run verify:deployment
```

### 4. デプロイメント状態の一元管理

```
deployments/
├── current.json        # 現在のデプロイメント（シンボリックリンク）
├── history/           # 過去のデプロイメント
│   └── bonsoleil-2025-01-07-12-00-00.json
└── README.md          # デプロイメント履歴の説明
```

### 5. ドキュメントの統合

- `/docs/DEPLOYMENT.md` - 統一されたデプロイガイド
- 古いガイドは削除またはアーカイブ
- 明確な手順とトラブルシューティング

## 実装手順

### Phase 1: 現状の整理（低リスク）
1. 使用されていないスクリプトを`archive/`に移動
2. 重複ドキュメントを統合
3. 現在のデプロイ状態を確定

### Phase 2: 新システムの構築（中リスク）
1. 新しいディレクトリ構造を作成
2. 統一デプロイスクリプトを実装
3. 設定ファイルシステムを導入

### Phase 3: 移行と検証（高リスク）
1. 新システムでテストデプロイ
2. 既存のデプロイメントとの互換性確認
3. 完全移行

## メリット

1. **明確性**: どのコマンドを使うべきか一目瞭然
2. **保守性**: 単一のコードベースで管理
3. **追跡性**: デプロイ履歴が明確
4. **信頼性**: エラーが起きにくい
5. **拡張性**: 新しいネットワークの追加が容易

## 注意点

- 既存のデプロイメントとの互換性を保つ
- 段階的な移行で本番への影響を最小化
- チーム全員への周知とトレーニング

## タイムライン

- Phase 1: 1日
- Phase 2: 2-3日
- Phase 3: 1-2日
- 合計: 1週間程度

このリファクタリングにより、デプロイプロセスが安定し、開発効率が大幅に向上します。